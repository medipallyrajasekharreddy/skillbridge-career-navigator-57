import{createFileRoute}from"@tanstack/react-router";import{JobDetail}from"../components/app-views";import{pageMeta}from"../components/meta";export const Route=createFileRoute("/jobs/$jobId")({head:()=>pageMeta("Job skill comparison","Compare your skills against this job's requirements."),component:Page});function Page(){const{jobId}=Route.useParams();return <JobDetail jobId={jobId}/>}
